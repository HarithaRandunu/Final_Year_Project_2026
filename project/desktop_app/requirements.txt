fastapi>=0.115
uvicorn>=0.32
pandas>=2.2
plotly>=6.0

# Optional - only for the legacy native-window mode (python main_desktop.py).
# The web app (python main.py) does not need them; uninstalling them is safe.
#   pywebview>=5.3
#   pythonnet>=3.1.0; sys_platform == "win32"
