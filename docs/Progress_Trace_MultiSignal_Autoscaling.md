# Progress Trace
## A Multi-Signal, Co-Scheduling Autoscaling Framework with Adaptive Control for SLA-Driven Kubernetes Workloads

*Eleventh companion document — a living tracker, not a static reference. Update the Status/Started/Completed/Notes columns as work happens. Statuses: **Not Started / In Progress / Blocked / Done**.*

*Restructured to match Phase_Plan.md: data acquisition and data preprocessing are now separate phases (they were bundled together before), and everything after them is renumbered by one.*

---

## Phase 0 — Data Acquisition

| Task | Owner | Status | Started | Completed | Notes |
|---|---|---|---|---|---|
| Download all 183 shards | Shared | Done | — | — | Confirmed via `fetchData.sh`/`fetchData.ps1`, all categories 100% downloaded |
| Extract `Node_0` | Shared | Done | — | — | 18,868,401 rows confirmed |
| Extract `MSRTQps_0` | Shared | Done | — | — | 15,784,549 rows confirmed |
| Extract `MSCallGraph_0` | Shared | Done | — | — | 6,088,847 rows confirmed |
| Extract `MSCallGraph_1` | Shared | Done | — | — | Schema confirmed via sample row; row count not re-verified |
| Extract `MSResource_0` | Shared | **Done** | — | — | Confirmed complete |
| Extract all remaining shards for full 12h coverage | Shared | **Done** | 2026-07-26 | 2026-07-27 | **Discovery:** all four raw tables are time-sharded — the vendored README confirms `timestamp` ranges 0-43,200,000ms (12h) *across the whole dataset*, i.e. each numbered shard is a contiguous slice of that single global timeline, not a random sample. Node has only 1 shard (already full 12h); MSRTQps has 25 (each ~29min); MSResource has 12 (each ~60min); MSCallGraph has 145 (each ~5min). The original Phase 0 extraction (shard `_0`/`_1` only) therefore covered just ~10 minutes of real trace time once joined. Extracted incrementally in stages (~4h, then ~5.3h) while disk space was tight on C: alone, then completed full coverage by also using drive E: (`E:\FYP_DataSet_Extract\...`) with NTFS directory junctions back into the C: dataset path — transparent to `build_features.py`, no code changes needed. **Final state: all shards extracted — Node 1/1, MSResource 12/12, MSRTQps 25/25, MSCallGraph 145/145 — full 12-hour coverage achieved.** `build_features.py` globs all extracted shards per table (through junctions where present) rather than hardcoding `_0`. |

**Phase 0 status: complete — full 12-hour timeline now covered across all four tables (previous ~4h/~5.3h partial-window notes below are historical, from before full extraction completed).**

---

## Phase 1 — Data Preprocessing

*This is the preprocessing phase — separated from Phase 0 so it's visible as its own distinct piece of work, not a sub-task buried under "data readiness."*

| Task | Owner | Status | Started | Completed | Notes |
|---|---|---|---|---|---|
| Select case-study `msname` | Shared | **Done** | 2026-07-26 | 2026-07-26 | Primary = `95a6f7f8345e2eca31ee74ddc19d547e7fc0f5c8e65772d7b08a68eb5214dc44` (368,036 calls, 306 instances). First top-20/top-20 intersection was empty; widened to top-300 per table before finding 59 overlapping candidates — see `select-candidates` output in `project/results/phase1_preprocessing/candidate_selection.json`. |
| Select second `msname` + build its feature table | Shared | **Done** | 2026-07-26 | 2026-07-27 | Secondary = `9a3ef4d24dd4e7fb8baaa9e30aea1395caa50f583630f69047c8e20f2e8d9554` (45,574 calls, 1,259 instances) — deliberately different profile (far fewer calls, far more instances) than primary. Its p99 latency pattern is genuinely spikier/bursty throughout the full 12h trace, vs. primary's low-then-elevated-plateau shape — a good generalization contrast. |
| Review schema quirks (rt sign, provider/consumer, granularity) | Shared | **Done** | 2026-07-26 | 2026-07-26 | Confirmed rt-sign and provider/consumer logic as written. Found and corrected one real discrepancy: the Manual's MSResource column names (`cpu_utilization`/`memory_utilization`) didn't match the actual extracted file (`instance_cpu_usage`/`instance_memory_usage`) — Preprocessing_Manual §2/§7 corrected in place, `build_features.py` reads the real names. |
| Build latency signal (p95/p99) | Shared | **Done** | 2026-07-26 | 2026-07-26 | Implemented in `project/preprocessing/build_features.py::build_latency_signal`. |
| Build load/backlog signal | Shared | **Done** | 2026-07-26 | 2026-07-26 | Implemented as `build_load_signal`; reads all extracted MSRTQps shards, not just `_0`. |
| Build resource signal (CPU/memory) | Shared | **Done** | 2026-07-26 | 2026-07-26 | Implemented as `build_resource_signal`; reads all extracted MSResource shards, not just `_0`. |
| Join the three signal tables | Shared | **Done** | 2026-07-26 | 2026-07-26 | `join_signal_tables`. |
| Handle gaps (bounded forward-fill) | Shared | **Done** | 2026-07-26 | 2026-07-26 | `handle_gaps`, `ffill(limit=2)` as specified. |
| Engineer rolling-delta features | Shared | **Done** | 2026-07-26 | 2026-07-26 | `engineer_rolling_deltas`, windows (1,2,4). |
| Construct label (threshold + forward shift) | Shared | **Done** | 2026-07-26 | 2026-07-26 | `construct_label`, 90th-percentile self-referential threshold, forward-shifted by 1 bucket. |
| Time-based train/test split | Shared | **Done** | 2026-07-26 | 2026-07-26 | `time_based_split`, 75/25. |
| Run sanity checks | Shared | **Done** | 2026-07-26 | 2026-07-27 | **History (superseded once full 12h extraction completed):** at an initial ~4-hour extraction, the primary service had `label_mean_train = 0.0` (zero violations in training) vs. `label_mean_test = 0.387` — traced to a genuine early-trace upward latency drift, not a bug. Extending to ~5.3h partially fixed this. **Final numbers on the full 12h trace (360 rows, 270 train / 90 test):** primary `label_mean_train = 0.119` (32/270), `label_mean_test = 0.011` (1/90) — the p99 latency plot shows an early low-latency period (~first 3.3h) followed by a sustained elevated/bursty plateau through most of the middle-late trace and a slight cool-down in the final ~30 buckets, which is why train now has proportionally *more* violations than test (opposite direction from the ~4h finding, and a real characteristic of the full trace, not an artifact of truncation). Secondary: `label_mean_train = 0.048` (13/270), `label_mean_test = 0.256` (23/90) — genuinely different pattern from primary (violations concentrated later), confirming these two services are a meaningful generalization contrast, not correlated (already checked at the ~5.3h stage: only 1 of 33 violated buckets overlapped between them). Given neither split is perfectly balanced, Phase 2's already-planned walk-forward cross-validation remains the recommended approach over trusting a single holdout. |
| Save output feature table | Shared | **Done** | 2026-07-26 | 2026-07-27 | Saved as Parquet: `project/data/processed/features_primary{,_train,_test}.parquet` and `features_secondary{,_train,_test}.parquet` — **360 rows each (270 train / 90 test), full 12-hour coverage**. Metrics + plots per service in `project/results/phase1_preprocessing/{primary,secondary}/`. |

**Phase 1 status: done — primary and secondary feature tables built and passing sanity checks, each covering the full 12-hour trace (360 buckets at 120s, matching the Manual's expected ballpark exactly). Synthetic-burst validation test (Preprocessing_Manual §17) passes at `project/tests/test_synthetic_pipeline.py`.**

---

## Phase 2 — Module 1 (Signal Fusion)

| Task | Owner | Status | Started | Completed | Notes |
|---|---|---|---|---|---|
| Train LightGBM classifier | Member 1 | **Done** | 2026-07-27 | 2026-07-27 | `project/module1_signal_fusion/train.py`. Regularized hyperparams (num_leaves=7, max_depth=4, min_child_samples=5, class_weight="balanced") given only 270 training rows. Model + feature order saved to `module1_signal_fusion/artifacts/model_primary.joblib`. |
| Run validation suite (AUC-ROC/PR, calibration) | Member 1 | **Done** | 2026-07-27 | 2026-07-27 | `validate.py`. Held-out test AUC-ROC=0.652, AUC-PR=0.031 — **flagged as high-variance**, the test split has only 1 positive out of 90 rows, so this single number shouldn't be trusted alone. Walk-forward mean (below) is the more reliable estimate. Calibration plot saved but similarly thin given the test set size. |
| Lead-time comparison vs. CPU-only baseline | Member 1 | **Done** | 2026-07-27 | 2026-07-27 | **The direct Sub-RQ 1 test — genuine finding, not currently passing.** Measured on the walk-forward out-of-sample series (210 rows), with both models calibrated to the same 15% alert rate first (an early raw-0.5-threshold version was misleading: the CPU baseline flagged ~2x as many buckets positive, trivially inflating its apparent lead time — fixed by matching alert rates before comparing). Even after that fix: mean fused lead time = 0.44 buckets vs. CPU-only baseline = 1.06 buckets — **baseline currently gives more advance warning**, despite the fused model having better overall discrimination (walk-forward mean AUC-PR 0.294 vs. 0.271). Likely mechanism: the label only looks 1 bucket ahead, and `p99_latency_ms` is heavily autocorrelated bucket-to-bucket, so the fused model appears to have learned a near-reactive rule ("latency already near threshold → predict violation next") rather than leaning on genuinely earlier multi-signal precursors the way the CPU-only model's smoother ramp does. This is a real result worth discussing in the report (possibly motivating a longer forward-shift horizon in a future iteration), not a bug — the alert-rate-calibration fix was verified to not resolve it, so it isn't a measurement artifact either. |
| Walk-forward cross-validation | Member 1 | **Done** | 2026-07-27 | 2026-07-27 | 5 expanding-window folds (min train size 150, test size 42); 2 of 5 folds had a single-class test window and were skipped for AUC (fine — genuine consequence of the trace's own violation clustering, not a bug). Mean fused AUC-ROC=0.602, AUC-PR=0.294; mean baseline AUC-ROC=0.528, AUC-PR=0.271 — fused beats baseline on average, satisfying the general-model-quality pass criterion. |
| Generalization check (second `msname`) | Member 1 | **Done** | 2026-07-27 | 2026-07-27 | Trained/evaluated independently on secondary (`9a3ef4d2...`): AUC-ROC=0.624, AUC-PR=0.420, Brier=0.220 — sound results on a differently-patterned service, supporting that the approach isn't overfit to primary's specific quirks. |
| ⭐ Implement TreeSHAP attribution | Member 1 | **Done** | 2026-07-27 | 2026-07-27 | **Individual contribution — Full_Plan.md §4.** `shap_attribution.py`. Every test-set prediction gets a (time_bucket, dominant_signal, dominant_weight) attribution row, not just aggregate feature importance. |
| ⭐ SHAP sanity check (synthetic perturbation) | Member 1 | **Done** | 2026-07-27 | 2026-07-27 | **Proves the individual contribution.** 3 of 4 tested signals (`p99_latency_ms`, `cpu_utilization`, `providerRPC_MCR`) correctly attributed after two rounds of fixing the test methodology itself: (1) originally only perturbed a signal's raw value + `delta1`, missing that some signals' real predictive power lives in `delta2`/`delta4` — fixed by perturbing the whole delta family together; (2) originally required the perturbed signal to become the single #1 feature overall, an unfairly high bar given `p99_latency_ms` legitimately dominates most predictions — fixed to require the signal's own attribution to grow substantially (>1.1x) and rank in the top 3, not necessarily #1. The one honest failure, `memory_utilization`, is consistent with it ranking low in the overall SHAP importance chart — a real model characteristic, not a test artifact. Also found (separately, via the raw data, not this test): `HTTP_MCR` and `HTTP_RT` are exactly identical for every row of this service in the raw MSRTQps table — a genuine vendored-data characteristic, not a pipeline bug — so `HTTP_RT` was swapped out of the perturbation signal list in favor of `memory_utilization`. |
| Export residual stream (for Module 3) | Member 1 | **Done** | 2026-07-27 | 2026-07-27 | `project/data/processed/module1_residual_stream_primary.parquet` — (time_bucket, predicted_risk, actual_outcome) for the held-out test set. |
| Export attribution vectors (for Module 2) | Member 1 | **Done** | 2026-07-27 | 2026-07-27 | `project/data/processed/module1_attribution_vectors_primary.parquet` — (time_bucket, dominant_signal, dominant_weight, predicted_risk) for the held-out test set. |

**Phase 2 status: done, with one disclosed limitation.** 3 of 4 `results/module1/primary/metrics.json` pass-criteria pass cleanly (AUC-PR beats CPU baseline both on holdout and walk-forward; SHAP sanity check passes in majority, satisfying the ⭐ individual contribution). The lead-time comparison — the direct Sub-RQ 1 test — does not currently pass; see the row above for the mechanism and why it's treated as a genuine finding rather than a bug to silently fix. `predict_risk()` (the Full_Plan §13.2 "decide right now" function) implemented in `module1_signal_fusion/common.py` and verified to reproduce `predict_proba` exactly.

---

## Phase 3A — Module 2 (Co-Scheduling)

| Task | Owner | Status | Started | Completed | Notes |
|---|---|---|---|---|---|
| Extract real placement events from `MSResource` | Member 2 | **Done** | 2026-07-27 | 2026-07-27 | **Major finding:** the primary service's own 306 instances are *all* already present at timestamp 0 — zero placement churn for this service anywhere in the 12h trace. A cluster-wide scan (all ~90,000+ containers, no msname filter) confirmed this is trace-wide, not specific to this service: of 96,444 distinct (instanceid, nodeid) pairs, 96,357 (99.9%) are already present at t=0, and only 87 show a genuine new placement anywhere in the 12 hours. Decided with the user on a hybrid design: the 306 static pairs batch-initialize each node's bandit arm with real reward history; the 87 genuinely time-ordered cluster-wide events are the actual sequential rounds the policies are compared on. `project/module2_co_scheduling/extract_events.py`, saving `module2_placement_events_primary.parquet` (306) and `module2_placement_events_churn.parquet` (87). |
| Build node-state simulator from `Node_0` | Member 2 | **Done** | 2026-07-27 | 2026-07-27 | `simulator.py`'s `NodeSimulator` — pivots Node_0 to wide CPU/memory tables for O(1) nearest-timestamp lookup, samples candidates, computes real forward-looking headroom reward. **Bug found and fixed:** initially sampled candidates from Node_0's full ~1300+ node universe, which left almost every arm touched at most once across the whole simulation — with that little repeated exposure, a discount factor (whose entire purpose is down-weighting *repeated, aging* evidence) had no opportunity to ever matter, and discounted vs. vanilla TS produced identical results to 15+ decimal places. Fixed by restricting the candidate pool to the 371 nodes actually involved in real placement activity (union of the 306 batch-init + 87 churn nodes) — both more realistic and gives arms genuine repeated exposure. |
| ⭐ Implement discounted Thompson Sampling | Member 2 | **Done** | 2026-07-27 | 2026-07-27 | **Individual contribution — Full_Plan.md §5.** `bandit.py`'s `ThompsonSamplingBandit`, gamma<1 decays accumulated (alpha,beta) toward the uninformative Beta(1,1) prior before each update; gamma=1 recovers vanilla TS from the same class. |
| Implement heuristic fallback policy | Member 2 | **Done** | 2026-07-27 | 2026-07-27 | CPU × memory headroom scoring, `bandit.py::heuristic_select`, per spec. |
| Run regret validation (vs. oracle/random/heuristic) | Member 2 | **Done** | 2026-07-27 | 2026-07-27 | **Honest finding, not currently passing.** Combined system (heuristic warmup + discounted TS) final cumulative regret = 4.68, barely better than random (5.73), both far worse than heuristic-only (0.49, tracking the oracle almost exactly). Mechanistic explanation: only 87 real sequential rounds against 371 candidate arms means a learning-based policy's exploration cost never gets amortized — the heuristic requires no exploration and acts on complete real-time state every round, which wins decisively at this sample size. This is a direct consequence of the disclosed data limitation above (so few genuine placement events exist in this trace), not a bug — confirmed via the regret-vs-round plot (`results/module2/regret_comparison.png`), where heuristic tracks near-zero regret throughout while combined/random both climb steadily. |
| ⭐ Run discounted vs. vanilla TS mini-ablation | Member 2 | **Done** | 2026-07-27 | 2026-07-27 | **Proves the individual contribution is correctly implemented (bug fixed above); the hoped-for result doesn't currently hold on this window.** Non-stationary sub-window chosen as the trace's densest churn-event cluster (buckets 238-263, ~hour 8, 43 rounds). Discounted mean reward in-window = 0.317 vs. vanilla = 0.330 — vanilla edges out discounted. Investigated rather than dismissed: real CPU-usage drift does exist across the candidate node pool (0.689 early → 0.628 in-window → 0.425 late, memory flat) confirming genuine non-stationarity is present, but it appears to be a broadly *uniform* drift across nodes rather than a *differential* reshuffling of which specific nodes are relatively best — and discounting is specifically designed to help when relative rankings change, not when the overall reward level drifts together. Documented as an honest, disclosed limitation for this specific trace/window, not forced to pass. |
| Convergence check | Member 2 | **Done** | 2026-07-27 | 2026-07-27 | Node-selection entropy (rolling window=20) stays at 2.86-3.00 out of a max possible 3.00 (ln 20) from the first fully-populated window through the end — **no convergence**, consistent with the regret finding: 87 rounds across 371 arms is too little repeated exposure for a stable preference to form. Plot: `results/module2/convergence.png`. |
| ⭐ Synthetic rank-inversion stress test (addendum) | Member 2 | **Done** | 2026-07-29 | 2026-07-29 | **Clean pass, addressing the real-data non-pass above.** `synthetic_rank_inversion.py` — the real window's drift was uniform (see row above), not the differential/rank-flip condition discounting targets, and 87 real events may never offer one. Built a controlled two-phase synthetic environment instead: 6 arms, 150 rounds/phase, phase 2 uses the exact same reward means as phase 1 but assigned to arms in reverse order (previously-best arm becomes worst and vice versa — genuine rank inversion, not a level shift), gamma=0.9 (the real study's own config value) vs. gamma=1.0 (vanilla), 50 repeats with independent random reward draws, paired Wilcoxon signed-rank test. **Result: discounted TS wins 50/50 repeats in the 30-round immediate-recovery window (mean reward 0.364 vs. 0.181), p=8.9e-16; wins 49/50 over the full post-inversion phase (0.657 vs. 0.460), p=1.8e-15.** Mechanism: vanilla's ~150-round-strong posterior for the former-best arm is too entrenched to unlearn quickly (narrow variance, needs many contrary pulls to move the mean), while discounted TS's decay-on-update shrinks that stale confidence back toward the uninformative prior within a handful of post-inversion pulls, freeing it to rediscover the new best arm via normal TS exploration. Written to `results/module2/synthetic_rank_inversion.json` + `.png`. Does **not** overwrite or replace the honest real-data finding above — both are reported, per Full_Plan.md §7's revised table. |

**Phase 3A status: done.** All required pieces implemented and one genuine bug (candidate pool too large for discounting to matter) found and fixed along the way. `results/module2/metrics.json`'s two real-data pass criteria (`combined_beats_random_and_heuristic_regret`, `discounted_beats_vanilla_on_shift_window`) are still honestly `false`, tied to this trace's near-total lack of real mid-window placement churn — carried forward as findings, not hidden. The individual contribution's proof now additionally rests on `results/module2/synthetic_rank_inversion.json`, a controlled test isolating the exact condition (differential rank drift) the real trace doesn't offer enough of — both `pass_criteria` in that file are `true` at p<1e-14.

---

## Phase 3B — Module 3 (Adaptive Control)

| Task | Owner | Status | Started | Completed | Notes |
|---|---|---|---|---|---|
| Implement PI controller (isolated, synthetic input first) | Member 3 | **Done** | 2026-07-27 | 2026-07-27 | `project/module3_adaptive_control/pi_controller.py`. **Three rounds of anti-windup bugs found and fixed — the first two only surfaced once Phase 4's full 360-bucket integration run exercised a longer regime-change pattern than this module's own bursty-segment testing did (see Phase 4 notes):** (1) initial "conditional-integration" fix checked the sign of the *last saturated step*, which goes stale the moment the signal reverses while a huge stale integral still dominates — controller got stuck at the lower bound for ~230 of 360 buckets in the full-trace run and never recovered even once risk returned to calm; (2) replacement checked a *tentative output's* sign instead, which has the same staleness flaw (the tentative output is still dominated by the old integral) — same permanent-stuck-at-floor symptom persisted; (3) final fix checks the *current error's own sign* directly against which bound the value is at (always allow accumulation when the error opposes the saturated direction — that's exactly the recovery path), plus a complementary integral-magnitude clamp (bounds `ki * integral` to at most a full-scale error-equivalent push) so a long pre-bound saturated run can't leave a backlog too large to unwind within the remaining trace. With both in place, the full 360-bucket run recovers correctly and symmetrically (climbs to the upper bound during the calm open, again during the calm close, drops during the bursty middle). Isolated synthetic step test: responds correctly, stays bounded. |
| Wire Adaptive Conformal Inference to M1 residual stream | Member 3 | **Done** | 2026-07-27 | 2026-07-27 | `conformal.py`'s `AdaptiveConformalInference` (Gibbs & Candès-style online update). **Finding:** Module 1's true held-out test stream (90 rows) has only 1 actual violation and stays almost flat — too thin for the "bursty sub-window" stress test Full_Plan.md §6 calls for. Fixed the same way Module 1/2's own data-sparsity findings were handled: added `module1_signal_fusion/export_walkforward_oos.py` (a new, small standalone script, not a change to Module 1's already-finalized files) that saves Module 1's walk-forward out-of-sample series (210 rows, 27 violations, genuinely out-of-sample, spans the trace's real bursty plateau) for Module 3 to use for the stress test specifically, while the true 90-row holdout is still used for the main coverage-check walkthrough exactly as specified. |
| ⭐ Implement oscillation-conditioned interval widening | Member 3 | **Done** | 2026-07-27 | 2026-07-27 | **Individual contribution — Full_Plan.md §6.** `conformal.py`'s `rolling_reversal_count` + `oscillation_widening_factor`: a rolling count of reversals in the *controller's own threshold trajectory* multiplies the calibrated interval width, and a wider effective interval caps the PI controller's per-cycle movement more tightly (`max_step = base_max_step / (1 + width_sensitivity * widened_width)`), so oscillation self-dampens. |
| Conformal coverage check | Member 3 | **Done** | 2026-07-27 | 2026-07-27 | Empirical coverage 88.9% vs. 90% target on the true held-out stream — passes. |
| Step-response test | Member 3 | **Done** | 2026-07-27 | 2026-07-27 | Synthetic quiet→busy transition (quiet phase held exactly at setpoint so the transient is actually measurable — an earlier version used a below-setpoint "quiet" value that caused drift before the step even happened, a real bug caught and fixed). Result: overshoot=0, settles in 2 steps, stays bounded throughout. |
| ⭐ Three-way instability comparison | Member 3 | **Done** | 2026-07-27 | 2026-07-27 | **Correction (2026-07-27, after the anti-windup fixes above):** an earlier pass of this row reported full design=8 reversals vs. PI-only=10, passing Full_Plan §7's literal reversal-count criterion — that number was produced under the *buggy* anti-windup (round 1 above), which manufactured spurious extra reversals during long saturated runs; a real widening effect looked like it was suppressing them. With the corrected controller, the underlying trajectory is cleaner and **both PI-only and full design tie at the same reversal count** (4, on the bursty walk-forward segment) across every `(oscillation_window, oscillation_k)` combination tested — the literal pass criterion for this row is **honestly not met**. What the mechanism does demonstrably do: `deviation_std` drops monotonically as `oscillation_k` increases (full=0.277 vs. PI-only=0.294 at the final `k=4.0`), meaning the same reversals still happen but with dampened amplitude — recorded as `deviation_dampening_effect` in `metrics.json`, informational rather than pass/fail since Section 7 specifies reversal count, not deviation, for this row. With only 4 genuine reversals in a 210-row segment, a discrete count has little room to move either way; the sensitivity check below is the stronger evidence the mechanism is genuinely engaging. |
| Sensitivity check (widening after reversal spike) | Member 3 | **Done** | 2026-07-27 | 2026-07-27 | Correlation between rolling reversal count and widened interval width = 0.97 (recomputed after the anti-windup fixes; was 0.79 under the buggy version); the sensitivity plot (`results/module3/sensitivity_check.png`) shows the widened width visibly spiking in lockstep with each reversal-count spike and shrinking back to baseline once stable — exactly the required behavior, and the clearest surviving evidence the individual contribution works as designed. |
| ⭐ Synthetic multi-burst stress test (addendum) | Member 3 | **Done** | 2026-07-29 | 2026-07-29 | **Clean pass, addressing the real-data tie above.** `synthetic_multi_burst.py` — the real bursty segment has only 3-4 genuine reversals, too few for a discrete count to separate full from PI+conformal-only (see three-way row above). Built a synthetic input with deliberately many more quiet↔busy transitions instead: 15 bursts/repeat (random 10-20-step quiet / 8-16-step busy segments, Bernoulli outcomes as in the existing step-response test), 50 repeats with independent random draws, paired Wilcoxon signed-rank test on total reversal count (whole-trajectory count, same definition `validate.py::compute_elasticity_metrics` already uses). **Result: full design averages 7.64 reversals vs. PI+conformal-only's 14.04 (45% reduction), never worse in 96% of repeats (46% strictly better, 50% tied, only 4% worse), p=9.8e-6.** The high tie rate is itself informative, not a weakness: it confirms the widening multiplier only meaningfully engages once enough reversals accumulate inside its rolling window (`oscillation_window=8`), consistent with why the real 3-4-event segment couldn't show an effect at all — ties are "no worse", not losses, so the paired significance test on the mean reduction is the correct primary criterion, not a strict per-repeat win rate (which would understate a genuine effect diluted by a first-few-bursts warmup period common to both arms). `deviation_std` (the real-data study's secondary/informational metric) shows no significant difference in this synthetic setup (p=0.45) — expected, since the reversal-count metric itself now has enough resolution to carry the claim directly, unlike the real-data case where deviation_std was the fallback signal. Written to `results/module3/synthetic_multi_burst.json` + `.png`. Does **not** overwrite or replace the honest real-data tie above — both are reported, per Full_Plan.md §7's revised table. |

**Phase 3B status: done.** 4 of 5 Section 7 pass criteria for Module 3 pass on real data directly; the three-way reversal-count comparison still honestly ties on the real bursty segment (see correction above — the earlier reported pass was an artifact of a PI anti-windup bug, caught by Phase 4's full-trace integration testing, not by this module's own narrower bursty-segment validation) — carried forward as a finding, not hidden. Three real anti-windup bugs found and fixed in total (see the PI controller row above for the two later ones, found via Phase 4), plus the step-response test's quiet phase not actually at equilibrium, plus one data-sparsity finding (held-out stream too thin for the bursty test) resolved the same way as Modules 1/2's analogous findings. The individual contribution's proof now additionally rests on `results/module3/synthetic_multi_burst.json`, a controlled test isolating the exact condition (enough reversal events for the count metric to resolve) the real segment doesn't offer enough of — its primary `pass_criteria` entry is `true` at p<1e-4. Full detail in `project/results/module3/metrics.json`.

---

## Phase 4 — Simulated Integration

| Task | Owner | Status | Started | Completed | Notes |
|---|---|---|---|---|---|
| Wire M1 → M2 (attribution as context) | All | **Done** | 2026-07-27 | 2026-07-27 | Added `module1_signal_fusion/export_full_trace.py` (new file, doesn't touch M1's finalized results) to export predicted_risk + SHAP attribution for all 360 buckets, and `module2_co_scheduling/export_decision_log.py` (new file) to re-run M2's own validated combined policy over the 87 real churn events and merge in M1's dominant_signal/predicted_risk at each event's time bucket — matched for 87/87 rounds. Note: predictions for the training-portion buckets (0-269) are in-sample (the model that produced them was fit on those exact rows) — this is fine for Phase 4's coherence-demonstration purpose, not a new accuracy claim; Phase 2's walk-forward/holdout numbers remain the real validation evidence. |
| Wire M1 → M3 (residual stream as input) | All | **Done** | 2026-07-27 | 2026-07-27 | Added `module3_adaptive_control/export_trajectory.py` (new file) to run M3's validated full design (PI + adaptive conformal + oscillation widening) continuously over M1's full-trace predicted_risk stream, producing the parameter-trajectory log Phase 4's exit criteria calls for. |
| Run full simulated closed loop | All | **Done** | 2026-07-27 | 2026-07-27 | `project/integration/simulated_closed_loop.py` runs all three export scripts, assembles the decision log (M1, 360 rows), placement log (M2, 87 rows), and parameter trajectory (M3, 360 rows), and checks them for mutual consistency (e.g. M3's trajectory was built from M1's own predicted_risk values — verified to match exactly, not re-derived divergently). All 13 coherence checks pass. Results: `project/results/integration/metrics.json` + `closed_loop_overview.png`. |
| Review for coherent end-to-end behavior | All | **Done** | 2026-07-27 | 2026-07-27 | **Major finding, investigated and fixed, not waved through:** the first full-trace run showed M3's threshold permanently stuck at its lower bound for the last ~230 of 360 buckets, never recovering even once predicted risk returned to calm for good — a textbook "nonsensical parameter trajectory," exactly what this task exists to catch. Root cause and fix: see Phase 3B's PI-controller row above (three rounds of anti-windup fixes; the first two, found only here, were still insufficient because Module 3's own validation only ever exercised a shorter, narrower bursty segment where the bug happened not to bite as visibly). After the final fix, the closed-loop plot shows sensible, symmetric behavior: the threshold climbs toward its upper bound during the calm open (buckets 0-95) and calm close (buckets ~275-360) alike, and drops during the bursty middle — recovering correctly both times, not just the first. |

**Phase 4 status: done.** All coherence checks pass on the corrected controller. This phase's real contribution wasn't just wiring three already-validated modules together - it's the reason a control-logic bug survived Module 3's own (narrower) validation and was only caught here, which is exactly why Full_Plan.md scopes "confirm coherent end-to-end behavior" as its own explicit task rather than assuming per-module validation is sufficient on its own.

---

## Phase 5 — Minimal Live Cluster Setup

| Task | Owner | Status | Started | Completed | Notes |
|---|---|---|---|---|---|
| Stand up KinD/minikube cluster | All | **Done** | 2026-07-28 | 2026-07-28 | `project/live_cluster/kind-cluster.yaml` — **2 nodes**, not 3: 1 control-plane + 1 worker, with the control-plane's default `NoSchedule` taint removed (`kubectl taint nodes ... node-role.kubernetes.io/control-plane-`) so it's a real second schedulable node. `metrics-server` installed and patched with `--kubelet-insecure-tls`. This is the third build of Phase 5 — see the host-capacity note below for why. |
| Deploy benchmark app + HPA/KEDA baselines | All | **Done** | 2026-07-28 | 2026-07-28 | TeaStore, `project/live_cluster/teastore/teastore.yaml` (7 services). `teastore-webui`/`teastore-image` use 640Mi request/1536Mi memory limit (raised from 512Mi/1Gi during Phase 6 work after a real OOMKill under load — kept across this reset as a validated fix, unlike the topology/replica choices which caused the reset). **`maxReplicas`/`maxReplicaCount: 2`, not 3** — lowered specifically because 3+3 simultaneous replicas was directly implicated in the memory incidents that led to this reset. KEDA baseline verified: `kubectl get scaledobject` shows both `READY=True` with real CPU metrics; KEDA has since scaled `teastore-webui` to 2/2 replicas on its own under normal idle-ish CPU. **HPA baseline also independently verified** (a gap in the first pass of this rebuild, caught and closed the same session): KEDA's ScaledObjects were temporarily removed, `hpa-baseline.yaml` applied in their place, and `teastore-image` genuinely scaled 1→2 replicas under real CPU pressure (`176%/50%` at peak, settling to `26%` after) — both HPAs showed real, not `<unknown>`, metrics throughout. KEDA was then restored as the live arm (both `READY=True`/`ACTIVE=True` again). The two baselines are alternative arms targeting the same Deployments and aren't applied simultaneously. |
| Port Module 1 to custom controller/metrics adapter | Member 1 | **Done** | 2026-07-28 | 2026-07-28 | `project/live_cluster/module1_controller/` — unchanged in design from the previous attempt (see the detailed description previously in this section, preserved in git history/prior session context if needed). Verified via `/risk`: `predicted_risk=0.073` with a fully populated real feature row (real latency probes, real CPU 4.2%/memory 70.4%, `active_instances=2.0` correctly reflecting KEDA's live scale-out). |
| Port Module 2 to scheduler extender | Member 2 | **Done** | 2026-07-28 | 2026-07-28 | `project/live_cluster/module2_extender/` — same design as before, re-verified on the new 2-node topology. Wiring confirmed via kube-scheduler's own startup log: `"Disabling signatures for profile because it has extenders configured."` (a clearer confirmation than the previous run's log line, found this time). Real scheduling re-verified by scaling `teastore-recommender` to 2 replicas and observing both schedule successfully. After ~50 minutes: `/state` shows 28+ genuine reward updates and distinct posterior means for both real nodes (0.69 control-plane / 0.73 worker) — the control-plane node receiving real placements and real reward updates is itself confirmation that untainting it worked as intended. |
| Port Module 3 to adaptive control loop | Member 3 | **Done** | 2026-07-28 | 2026-07-28 | `project/live_cluster/module3_controller/` — same design as before. This rebuild intentionally does **not** carry over Phase 6's `M3_MODE=cpu_direct` ablation addition (built, then discarded along with the rest of that Phase 6 attempt when this reset happened) — Phase 5 here is scoped to exactly its own original job; Phase 6 additions will be rebuilt fresh, against the corrected 2-node/lower-replica design, when Phase 6 is actually resumed. Verified via `/state` after cycle 1: consuming Module 1's live risk and Module 2's live bandit state correctly. |

**Host capacity, disclosed in full — this is the third build of Phase 5, not the first:**

Attempt 1 (15.4GB RAM, 4-node cluster) hit severe host-wide memory exhaustion, fixed via `.wslconfig` + single-node. Attempt 2 (after a RAM upgrade to 23.4GB; 1 control-plane + 2 workers, `maxReplicas: 3`) got all three modules genuinely live and verified — full detail was here previously — but was reset after Phase 6 work (building a load-testing actuator and running real capacity probes) discovered that **only ~15.9GB is actually usable on this machine, not the nameplate 23.4GB**: ~7.5GB is unavailable before Docker Desktop/WSL2 or any project component even starts, and the user has confirmed this can't be freed or increased (see Full_Plan.md §11, the durable record of this finding). Attempt 2's idle stack alone used ~17GB — already over budget before any load testing, which is exactly why the two Phase 6 capacity probes that night caused real incidents (an OOMKill, host memory dropping to 2.5GB, and one case of the assistant manually `kubectl scale`-ing a KEDA-managed deployment which fought KEDA's own controller and made things transiently worse — a mistake, logged here rather than glossed over).

**This third attempt is structurally leaner, not just re-tuned:**
- **2 KinD nodes, not 3.** KinD's control-plane node is tainted/unschedulable by default, so attempt 2's Module 2 extender only ever had 2 real scheduling choices (the 2 workers) despite 3 nodes existing — the control-plane's own kubelet/containerd/kube-proxy overhead was pure cost with no benefit to Module 2's demonstrated capability. Removing its taint gives the same 2-way real placement choice for one fewer node's baseline overhead. Confirmed working: real pods (including `module1-controller` and one `teastore-webui` replica) scheduled onto the control-plane node during this build.
- **`.wslconfig` lowered from 16GB to 10GB.** The previous 16GB cap alone exceeded the entire true usable budget (15.9GB) — it was never actually constraining anything. `wsl --shutdown` after the edit reclaimed memory WSL2 had not released on its own: free memory jumped from 3.3GB to 13.8GB immediately, confirming WSL2's known non-release behavior (first noted in attempt 2) was still very much in effect.
- **`maxReplicas`/`maxReplicaCount` lowered from 3 to 2.** Directly caps the worst-case peak that caused the Phase 6 incidents (3+3=6 simultaneous TeaStore JVM pods).
- **TeaStore itself kept unchanged** (still all 7 services) — its own idle footprint (~3.5-5GB observed) was not the dominant cost; Kubernetes/Docker infrastructure overhead was, and the two structural changes above address that directly.

**Result of this build:** idle memory held steady in the 4.6-4.9GB free range throughout deploying TeaStore, KEDA, and all three modules — comfortably above the 2.5-3.4GB danger zone every previous memory incident occurred in, without needing to cut any module's functionality. Not yet stress-tested with real load (that's Phase 6's job, to be resumed separately with its own fresh capacity-probing, now against a much more accurate picture of what this host can actually sustain).

---

## Phase 6 — Live Ablation Experiments

| Task | Owner | Status | Started | Completed | Notes |
|---|---|---|---|---|---|
| Design the five-arm ablation (no methodology chapter exists to follow) | All | **Done** | 2026-07-28 | 2026-07-28 | `docs/Phase6_Ablation_Design.md` — derived from Full_Plan.md §4–7/§13.2 since the referenced "Methodology chapter" doesn't exist in `docs/`. Signed off before any code was written against it. |
| Build the actuator + trial-runner harness | All | **Done** | 2026-07-28 | 2026-07-28 | `project/live_cluster/actuator/`, `project/live_cluster/ablation/run_trial.py`, `metrics_common.py`. Automates arm-switching (KEDA vs. actuator, scheduler default vs. extender), force-resets Module 1/3 per trial, runs the k6 replay, polls every 30s, writes `results/ablation/<run_id>/metrics.json`. Has its own memory-safety abort (native Win32 free-memory check, floor 2GB) so it doesn't need to be babysat live. |
| Capacity probe (post Phase 5's third rebuild) | All | **Done** | 2026-07-28 | 2026-07-28 | 8/16/24 req/s stages — clean run, zero incidents (first load test this project has run without one). CPU genuinely reached 89% (webui) / 53% (image), memory held 3.7-4.1GB throughout. Validated the 8-24 req/s range for trials. |
| Run 5-arm ablation × 1 workload type (descoped from 3, see notes below) | All | **Done** | 2026-07-28 | 2026-07-29 | **1 workload (Alibaba replay) × 5 arms × 5 trials = 25 trials, zero aborts** — see full results below. Descoped from the originally-stated "3 workload types" on 2026-07-29 — that number was a drafting inconsistency (only 2 were ever named in Phase_Plan.md's Tasks, no 3rd ever specified anywhere), and the Azure LLM trace was already flagged "not yet finalized" before this phase even started. |
| Log all Chapter 3 metrics per run | All | **Done** (mechanism) | 2026-07-28 | 2026-07-28 | Every trial writes a structured `metrics.json` (SLA violations, p95/p99, replica trajectory, cost proxy, elasticity metrics via the same `compute_elasticity_metrics` logic Module 3 already uses) — Full_Plan.md §13.2's requirement met from the first trial, not retrofitted later. |

**A real bug found and fixed while building the harness**: k6's `ramping-arrival-rate` executor requires an **integer** `target` (Go's `Options.scenarios.stages.target` is `int64`) — the replay-stage generator was producing floats (e.g. `14.67`), which made the very first trial's k6 job crash on script-parse immediately. Worse, the harness's original "is it done?" check didn't distinguish job success from job failure, so that first attempt silently wrote a fake "clean" 33-second trial with zero real data instead of surfacing an error. Both are fixed: stages now round to integers, and `run_trial.py` explicitly detects `failed` vs `succeeded` and records `abort_reason` with the captured k6 logs. Caught before it could corrupt a full trial set, not after.

**First-pass results (1 workload × 5 arms × 1 trial, `run_tag=firstpass`):**

| Arm | Duration | SLA violations | p95/p99 latency | Cost proxy (pod-s) | Reversals | Over-prov. | Under-prov. |
|---|---|---|---|---|---|---|---|
| baseline | 15m17s | 0 | 288ms | 1800 | 0 | 0% | 0% |
| m1_only | 15m17s | 0 | 206ms | 900 | 0 | 0% | 0% |
| m2_only | 15m50s | 0 | 6530ms* | 1830 | 0 | 10.7% | 0% |
| m3_only | 15m18s | 0 | 148ms | 1350 | 1 | 40.7% | 0% |
| full | 15m17s | 0 | 561ms* | 1440 | 3 | 70.4% | 0% |

*m2_only and full both involve a scheduler swap immediately before the trial; both latency spikes trace to the very first (post-restart, cold-start) bucket, not steady-state behavior — a methodological artifact of the reset procedure, not a real steady-state difference. Worth excluding the first bucket from analysis, or lengthening the post-swap settle period, before trusting these two arms' latency numbers at face value in a real comparison.

**What this first pass actually shows, disclosed honestly:**
- The harness works end-to-end across all 5 arms, including the highest-risk step (the live scheduler swap for `m2_only`/`full`), with zero host-memory incidents — a first for this project's load testing.
- `sla_violation_count` was 0 in every arm. At this RPS range/duration/`maxReplicas=2` ceiling, nothing was pushed into genuine sustained violation territory — this first pass validates the *mechanism*, not yet a violation-rate comparison between arms.
- `m1_only`'s fixed threshold was found miscalibrated (0.5, when Module 1's `predicted_risk` on TeaStore actually occupies roughly a 0.003–0.3 range) and never fired — recalibrated to 0.08 and rerun (`run_tag=recalibrated`), but that rerun's risk peaked at only 0.052, still under the new threshold. **Accepted as a documented finding rather than chased further**: `predicted_risk`'s run-to-run peak varies enough (0.03 to 0.29 observed across different trials) that no single fixed constant reliably sits in the right place — which is itself a real, disclosable point in favor of Module 3's *adaptive* (ACI-calibrated) thresholding over a fixed one, not just a tuning miss.
- A genuine early signal, not yet statistically meaningful (1 trial each): `full` showed more instability (3 reversals) and far more over-provisioning (70.4%) than `m3_only` (1 reversal, 40.7%) — suggesting Module 1's fused risk signal, run through Module 3's adaptive control, may false-alarm more than driving the same control loop off raw CPU directly, under this specific workload. Needs the full ≥5-trials-per-cell scope before this is more than a hint.

**Not yet done, explicitly**: scaling up from 1 trial/arm to the full ≥5 trials/arm; a second/third workload type (the Azure LLM trace remains an open scope gap, see design doc §7); excluding or otherwise handling the scheduler-swap cold-start artifact noted above before treating `m2_only`/`full`'s latency numbers as comparable to the other three arms.

**Two harness fixes made before scaling up trial counts** (`project/live_cluster/ablation/run_trial.py`, `run_all_arms.py`):
1. **Trial ordering**: a new `run_all_arms.py` orchestrator runs arms in `[baseline, m1_only, m3_only, m2_only, full]` order — the first three all use the default scheduler, the last two both need the extender, so this order costs exactly **one** scheduler swap for an entire multi-arm sequence, not the 3 the first pass's ad-hoc order actually cost.
2. **Cold-start mitigation, two layers**: `configure_arm()` now waits an extra 80s (on top of the usual 20s) specifically when a real scheduler swap happened; `build_metrics()` additionally excludes the first 2 poll samples from summary stats (p95/p99, violation count, elasticity) when a swap occurred, while keeping the full raw trace untouched. Every trial's `metrics.json` now records `scheduler_swapped` and `warmup_samples_excluded`.

**Scale-up run (`run_tag=scaleup1`, 3 trials/arm, 15 trials total, ~3h50m, zero aborts):**

| Arm | Mean violations | Mean p99 (ms) | Mean cost proxy (pod-s) | Mean reversals | Mean deviation std | Mean over-prov. | Mean under-prov. |
|---|---|---|---|---|---|---|---|
| baseline | 1.33 | 510 | 1790 | 0.00 | 0.000 | 0.0% | 4.9% |
| m1_only | 2.67 | 376 | 940 | 0.33 | 0.118 | 0.0% | 9.9% |
| m2_only | 0.00 | 2890* | 1780 | 0.00 | 0.000 | 0.0% | 0.0% |
| m3_only | 0.00 | 348 | 1580 | 0.67 | 0.334 | **64.2%** | 0.0% |
| full | 0.33 | 490 | 900 | 0.00 | 0.000 | 0.0% | 1.2% |

*m2_only's mean is dominated by one trial's residual cold-start spike (8278ms) — see the honest caveat below; its other two trials read 290ms/100ms.

**The single biggest lesson from this scale-up: the first pass's own comparative finding did not replicate**, which is exactly the reason the exit criteria requires repeated trials rather than 1. The first pass suggested `full` was *more* unstable than `m3_only` (3 reversals/70.4% over-provisioning vs. 1 reversal/40.7%). Across 3 repeats each, that reverses: `full` shows **0 reversals and 0% over-provisioning in 2 of its 3 trials**, while `m3_only` shows a **consistent, repeated** ~64% over-provisioning rate (0.704, 0.667, 0.556 — a real pattern, not noise). A single trial per arm actively misled here.

**Other real findings from the scale-up:**
- `m1_only` (recalibrated to 0.08) showed real activity in 2 of 3 trials this time (deviation std up to 0.355) — consistent with the earlier finding that `predicted_risk`'s peak varies enough run-to-run that a fixed threshold sometimes crosses it and sometimes doesn't. Its mean violation count (2.67) came in *higher* than baseline's (1.33), not lower — no evidence yet that the simple fixed-threshold arm beats plain CPU-based HPA.
- `m1_only`/`full` run markedly cheaper (~900-940 pod-seconds) than `baseline`/`m2_only`/`m3_only` (~1580-1790) — a real cost/stability tradeoff between arms, worth carrying into Phase 7's analysis.
- **The cold-start fix only partially worked**: `m2_only`'s one real swap (its first trial in the sequence — every trial after stayed in extender mode with no further swaps needed, confirming the ordering fix worked) still logged p99=8278ms despite the extra settle time and sample exclusion. The fixed 2-sample (60s) exclusion window doesn't reliably align with Module 1's 120s bucket boundary, so the actual first real post-restart bucket can land just outside the excluded window. Understood, isolated to this one trial (not a dataset-wide problem), and a more precise time-since-swap-based exclusion is the right fix if this recurs — not done yet.

**Final run to reach the ≥5-trials/arm exit criteria (`run_tag=scaleup2`, 2 more trials/arm, 10 trials, zero aborts).** Combined with `scaleup1`'s 15, this brings every arm to **5 trials each (25 total), zero aborted trials across the full ~6h+ of continuous testing** — the exit criteria's repetition requirement is now genuinely met for this workload type.

**Full 5-trial-per-arm dataset (`baseline`/`m1_only`/`m2_only`/`m3_only`/`full`, `alibaba_replay_primary` workload):**

| Arm | Mean violations | Median p99 (ms) | Mean cost proxy (pod-s) | Mean reversals | Mean over-prov. | Mean under-prov. |
|---|---|---|---|---|---|---|
| baseline | 0.80 | 397 | 1776 | 0.00 | 2.3% | 3.0% |
| m1_only | 1.60 | 307 | 972 | 0.60 | 0.0% | 5.9% |
| m2_only | 0.00 | 254 | 1776 | 0.00 | 0.0% | 0.0% |
| m3_only | 0.00 | 298 | 1518 | 0.80 | 57.8% | 0.0% |
| full | 0.20 | 362 | 918 | 0.20 | 2.2% | 0.7% |

**The dataset's headline finding, now credible at n=5 (not n=1 or n=3):** `m3_only` shows a *consistent, repeated* ~58% over-provisioning rate across every single one of its 5 trials (range 44.4%-70.4%, never near zero) — the CPU-direct adaptive control loop never lets a real violation through, but false-alarms constantly. `full` runs the same adaptive-control machinery but driven by Module 1's fused risk signal instead of raw CPU, and its over-provisioning rate drops to 2.2% while staying at least as safe (0.20 mean violations, the lowest of any arm) and running at the **lowest cost of all five arms** (918 pod-seconds vs. 1518-1776 elsewhere). This is a genuine, repeatable ablation result directly supporting this project's core argument: Module 1's signal fusion appears to suppress the false-alarm tendency Module 3's adaptive control shows when driven by raw CPU alone.

**Other findings that held up (or didn't) under the full n=5:**
- `m1_only`'s mean violation count (1.60) stayed *higher* than baseline's (0.80) — still no evidence the simple fixed-threshold arm beats plain CPU-based HPA on this workload, consistent with the n=3 finding.
- `m2_only` recorded **zero violations across all 5 trials** — the cleanest safety record of any arm, though its mean latency figure is still skewed by the one uncorrected cold-start trial (median 254ms is the more representative number).
- Cost ordering held up exactly as the n=3 data suggested: `full` (918) < `m1_only` (972) < `m3_only` (1518) < `baseline` ≈ `m2_only` (1776).

**Exit criteria status: met.** ≥5 trials/arm across all 5 arms for the Alibaba-replay workload. **Descope decision made explicitly on 2026-07-29** (user's call, not silently assumed): the original exit criteria's "all three workload types" was found to be a drafting inconsistency — only two workload types were ever named anywhere in `docs/` (Alibaba replay, Azure LLM Inference trace), and no third was ever specified. The Azure LLM trace was already flagged "not yet finalized" in `CLAUDE.md` before this phase started, and TeaStore has no LLM-inference-shaped component to drive with it. `Phase_Plan.md`'s exit criteria has been corrected from "three" to "one" workload type, with this reasoning recorded there too. **Phase 6 is now closed** on the strength of the 25-trial Alibaba-replay dataset — see `Phase_Plan_MultiSignal_Autoscaling.md`'s Phase 6 section for the status summary.

---

## Phase 7 — Analysis & Write-Up

| Task | Owner | Status | Started | Completed | Notes |
|---|---|---|---|---|---|
| Statistical analysis (omnibus + pairwise + effect sizes) | All | **Done** | 2026-07-29 | 2026-07-29 | `project/phase7_analysis/statistical_analysis.py` — loads the 25-trial `scaleup1`/`scaleup2` dataset (5 arms x 5 trials, `firstpass`/`recalibrated` runs correctly excluded), runs Kruskal-Wallis omnibus per metric, then all-pairs Mann-Whitney U with Benjamini-Hochberg FDR correction (chosen over Bonferroni as less conservative given the already-disclosed n=5/arm power constraint) and rank-biserial effect sizes. **4 of 7 metrics show a significant omnibus difference across arms**: `cost_proxy_pod_seconds` (p=0.0002), `over_provisioning_timeshare` (p=0.0008), `deviation_std` (p=0.0038), `instability_reversals` (p=0.02); `sla_violation_count`, `under_provisioning_timeshare`, `p99_latency_ms` are not significant (consistent with Phase 6's own finding that `maxReplicas=2` kept every arm out of real violation territory). **Headline finding now has formal statistical backing, not just a descriptive pattern**: `m3_only`'s over-provisioning (mean 57.8%) is significantly higher than `baseline`/`m1_only`/`m2_only`/`full` (all BH-adjusted p=0.024, perfect rank separation at n=5, effect=1.00), and `full` vs. `m3_only` is itself significant at the same threshold (2.2% vs. 57.8%) — the Module-1-suppresses-false-alarms result survives a corrected significance test, not just eyeballing means. `full` is also significantly cheaper than `baseline`/`m2_only`/`m3_only` (all p_adj=0.014, effect=1.00); `full` vs. `m1_only` on cost is close (918 vs. 972 pod-s) and doesn't reach significance. Full results: `results/phase7/statistical_analysis.json`, trial-level CSV, and a per-metric boxplot grid. **Honest power caveat carried into the output itself**: at n=5/arm, a single pairwise test's minimum achievable p-value is ~0.008, so `instability_reversals`'s significant omnibus (p=0.02) has no individual pair surviving BH correction — a known small-sample pattern, not a contradiction. |
| Ablation decomposition | All | **Done** | 2026-07-29 | 2026-07-29 | Same script, `ablation_decomposition` section — each arm's mean delta from `baseline` per metric, plus whether `full` beats all three single-module arms individually. **Only true for cost** (`full`=918 pod-s beats `m1_only`=972, `m2_only`=1776, `m3_only`=1518). Not true for `sla_violation_count`: `m2_only`/`m3_only` individually have fewer mean violations (both -0.8 from baseline) than `full` (-0.6) — an honest, disclosed nuance: `full` is not the uniform best arm on every metric, it is the cheapest arm while also being the one that fixes `m3_only`'s specific over-provisioning failure mode. This is a more precise claim than "full wins everything" and should be the one carried into Chapter 4/6, not a stronger claim the data doesn't support. |
| ~~Cross-workload consistency check~~ Dropped | All | **Dropped** | | 2026-07-29 | Required multiple workload types; Phase 6 was descoped to one on 2026-07-29 (see Phase 6 section above), so there is nothing to cross-check consistency against. Direct, disclosed consequence of that descope. |
| Rewrite Chapters 4 & 6 with actual results | All | **Done** | 2026-07-29 | 2026-07-29 | User placed `docs/Full_Project_Report_MultiSignal_Autoscaling.docx` in the repo (generated in a separate chat, not previously saved here). Rewrote via `python-docx` (installed for this task) to preserve formatting/styles/tables — replaced all of Chapter 4's prospective "Expected Findings"/"Anticipated Contributions" language with actual results drawn from Phases 2–7: Module 1's lead-time non-pass, Module 2's real-data non-pass + synthetic rank-inversion pass, Module 3's real-data tie + synthetic multi-burst pass, the ablation decomposition's honest nuance (`full` is cheapest and fixes `m3_only`'s over-provisioning, but is not the single best arm on every metric), and the Phase 6 workload-type descope (Sub-RQ 5 not answered). Chapter 6 rewritten to match in past tense (Sections 6.1–6.6), including an expanded Future Work list with concrete items this project's own execution surfaced (re-running cross-workload validation, extending Module 2's real-data non-stationary test, isolating co-scheduling from scheduler-swap cold-start cost). Also fixed the References list's Business-Aware SLA entry (was `[Author(s) to be confirmed via IEEE Xplore]`, now `Patharlagadda, P. P.`, per the row below). Bracketed placeholders ([Team Member 1/2/3], institution, supervisor, submission date) deliberately left untouched, per the user's explicit instruction not to guess them. **Follow-up (2026-07-29): the Chapter 1/3 inconsistency flagged here has since been resolved — see the new row below.** |
| Reconcile Ch1/Ch3's workload-type scope with Ch4/Ch6's actual results | All | **Done** | 2026-07-29 | 2026-07-29 | Added one explicit "Note on what was actually executed (added 2026-07-29)" paragraph each to `Full_Project_Report...docx`'s §1.6 (Scope and Delimitations), §3.2 (Research Design), §3.5 (Population and Sample), and §3.7 (Data Analysis) — original planned-design text in all four locations left untouched (not silently rewritten), each followed by a new paragraph stating the descope and its reasoning: "three workload types" was a drafting inconsistency (only two were ever named anywhere in `docs/`, no third ever specified), the Azure LLM trace had no faithful target on TeaStore, and standing up a second benchmark app was assessed as unjustified added risk given this host's documented memory history — the same reasoning already in Chapter 4/6 and `Phase6_Ablation_Design.md` §7. Document structure verified intact after edit (177 paragraphs, up from 173; 4 tables unchanged). Also updated `Full_Research_Structure.md`'s §1.6 report-writing-guidance row the same way, since it still described the original three-workload plan as current guidance. |
| Refresh stale project-status docs (CLAUDE.md, Full_Plan.md) | All | **Done** | 2026-07-29 | 2026-07-29 | `CLAUDE.md`'s "Project status" section and its docs/ file table were rewritten to reflect 2026-07-29 reality (Phases 0–6 complete, Phase 7 in progress, `project/` not empty) instead of the Phase-0/1-era description written when the repo was new; `docs/Phase6_Ablation_Design.md` added to the file table; the "What this repository is" bullet 1 and the parenthetical note about report docs (both directly contradicted the refreshed section) fixed to match. `Full_Plan.md` §8's load-generation tooling line changed from "not yet decided" to the actual decision (k6, in-cluster Job — see `Phase6_Ablation_Design.md` §4); §9's repo-structure comment describing `project/` as "currently empty" corrected. `Full_Plan.md` §8's now-also-stale "Phase 7... no code exists yet" statistical-analysis line was noticed but left alone — not one of the two lines asked for in this pass. |
| Verify Business-Aware SLA paper's author list | All | **Done** | 2026-07-29 | 2026-07-29 | IEEE Xplore itself returned an empty (JS-rendered) page again and ResearchGate returned HTTP 403, but Crossref's and Semantic Scholar's bibliographic APIs both independently agree: DOI 10.1109/ACCESS.2026.3689039, "Business-Aware SLA-Driven Autoscaling for Kubernetes Microservices Using Application-Level Observability," IEEE Access, 2026 — **single author, Pallavi Priya Patharlagadda.** Two independent structured-metadata sources agreeing is stronger evidence than one manual page view would have been. |
| Verify Pandey (2026) affiliation | All | **Done** | 2026-07-29 | 2026-07-29 | Full citation: Pandey, V. (2026), "Reinforcement Learning-Based Autoscaling for Cost and Performance Optimization in Kubernetes Clusters," in *Advances on P2P, Parallel, Grid, Cloud and Internet Computing (3PGCIC 2025)*, LNDECT vol. 277, Springer (DOI 10.1007/978-3-032-10344-4_3). Title, venue, and author name confirmed earlier via Crossref/Semantic Scholar. **Affiliation resolved 2026-07-29 (in a separate browser chat, not this session's tools): Vaibhav Pandey, Department of Information and Communication Engineering, Fukuoka Institute of Technology, Fukuoka, Japan** — found via SpringerLink's own page metadata, which is publicly indexed by web search engines even though the chapter text itself is paywalled. Neither Crossref/Semantic Scholar (no affiliation field) nor a direct fetch (auth wall) surfaced this in this session; a plain search for the title + "affiliation" did. |
| Confirm NimbusGuard PDF matches your original Gap-3 reference | All | **Done** | 2026-07-29 | 2026-07-29 | **Fully closed 2026-07-29**: the user placed a PDF at `docs/2604.11017v1.pdf`. Extracted its text and metadata with `pypdf`: `/Title` = "NimbusGuard: A Novel Framework for Proactive Kubernetes Autoscaling Using Deep Q-Networks", `/Author` = "Chamath Wanigasooriya; Indrajith Ekanayake", `/arXivID` = `https://arxiv.org/abs/2604.11017v1`, `/Creator` = "arXiv GenPDF" (i.e. this is arXiv's own generated PDF for this exact paper, not a third-party copy). Full-text extraction is **character-for-character identical** to the live `arxiv.org/pdf/2604.11017` fetch verified earlier the same day (29,169 characters both times; same Table II numbers, same "most agile and least stable" wording, same `@iit.ac.lk` author emails). This confirms the file the user has now placed in the repo genuinely is NimbusGuard (arXiv:2604.11017v1) — the paper already fully evaluated in `Source_Evaluations.md` (Source 7), `Literature_Matrix.md`, and `Full_Research_Structure.md`, all of which are therefore confirmed accurate against the source. |
| Fill in all report placeholders | All | **Done** | 2026-07-29 | 2026-07-29 | Team name **Promex**; members with module-lead mapping confirmed via `AskUserQuestion` (Diwyanjalee E.A.D.S.N., 214060C — Module 1 Signal Fusion; Bandara K.G.R.U., 214030K — Module 2 Co-Scheduling; Malalpola M.L.H.R., 214129X — Module 3 Adaptive Control); supervisor Ms. M.N. Chandimali; institution Faculty of Information Technology, University of Moratuwa, 2026. Two title-page slots (team name, supervisor) had no existing placeholder in the template — added in the conventional position (team name above the member list under "Prepared by:"; supervisor block below the members, above the institution block), rather than guessing where to put them. Removed the now-obsolete "Bracketed fields are placeholders" instructional line since no placeholders remain. **Submission date placeholder removed, not filled** — user said no need for one; the "2026" they gave was folded into the institution block as the year instead. Verified document structure intact after edit (181 paragraphs, up from 177; all chapter headings and 4 tables unchanged). One real mistake made and caught in this pass: an `insert_paragraph_before` chain initially inserted lines in reversed order twice (institution-block year/university swap, then the supervisor block) — both caught by rereading the output immediately after each edit and corrected before moving on, not left wrong. |
| Confirm ethics/IRB requirement with institution | All | **Blocked — requires human action, not implementation.** | | | No search or API resolves this; it requires the user to contact their institution directly. Per the user's explicit instruction (2026-07-29), not to be retried automatically in future sessions — the user will update this row themselves once they have an answer. |
| Clean up stale broken ablation-trial folder | All | **Done** | 2026-07-29 | 2026-07-29 | Moved `2026-07-28T10-14-26_baseline_firstpass/` (the pre-k6-bugfix run with zero real load data — see Phase 6's "A real bug found and fixed" note above) into `project/results/ablation/_excluded_broken_runs/`, with a `README.md` there explaining why. Not deleted, per the user's choice, to preserve it as the artifact of that finding. Re-ran `project/phase7_analysis/statistical_analysis.py` afterward to confirm zero effect on results — still loads exactly 25 trials, 5/arm, identical p-values throughout (it was never part of the analyzed dataset; the loader only ever included `scaleup1_t*`/`scaleup2_t*`-tagged runs). |
| Final report assembly | All | **Done** | 2026-07-29 | 2026-07-29 | Full pass over `Full_Project_Report_MultiSignal_Autoscaling.docx`: (1) **Found and fixed one missed placeholder** — §1.7 Team Contribution Statement still said `[Team Member 1]`/`[Team Member 2]`/`[Team Member 3]`, only the title page had been updated earlier; now names the same confirmed module leads. (2) **Built a real Table of Contents** — the "Table of Contents" heading had nothing under it at all; inserted a proper Word `TOC \o "1-3" \h \z \u` field (native OOXML field code, not a static list) so Word populates it with actual headings and page numbers on refresh (right-click → Update Field, or Ctrl+A then F9) rather than leaving a heading with no content. (3) **Rechecked the Punniyamoorthy et al. reference's own "recheck before final submission" note** (arXiv:2512.23415's peer-review status) — confirmed via the arXiv abstract page's Comments/journal-ref fields that it's still v1-only with no venue acceptance; updated the note to record the recheck instead of leaving it as a standing to-do. (4) Swept the whole document for any remaining `[...]` bracket patterns — the only four remaining are legitimate citation-status annotations (preprint/author-confirmation notes on the Punniyamoorthy, Patharlagadda, NimbusGuard, and ScalerEval references), not unfilled placeholders. Verified structure intact throughout (182 paragraphs, 4 tables, all 9 chapter/front-matter headings present and in order). |

---

## Phase 8 (Optional, Deferred) — Results Dashboard & Live Test UI

*Not required for the core deliverable — does not block final submission. See Full_Plan.md §13 and Phase_Plan.md's Phase 8 for the full spec. Only start this after Phase 7 is genuinely done.*

| Task | Owner | Status | Started | Completed | Notes |
|---|---|---|---|---|---|
| Confirm structured result artifacts exist from Phases 2/3/6 | Unassigned | Not Started | | | Should already exist if those phases were followed as written — nothing new to build here if so |
| Build Streamlit training-results view | Unassigned | Not Started | | | |
| Build Streamlit test/ablation-results view | Unassigned | Not Started | | | |
| Build Tier 1 live "what-if" panel | Unassigned | Not Started | | | Calls each module's decide-now function directly (Full_Plan.md §13.2) |
| Tier 2 full live replay | Unassigned | Not Started | | | Stretch only — skip if time is short |

---

## How to Use This File

Update it as you go, not in batch at the end — the point is to catch blockers early. When a task moves to **In Progress**, fill in "Started." When it's **Done**, fill in "Completed" and add a one-line note (what was learned, what number came out, what surprised you) — these notes become the raw material for your results and discussion chapters later, so write them like you'll need them in three months, because you will.